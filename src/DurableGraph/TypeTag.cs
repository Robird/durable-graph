namespace Atelia.DurableGraph;

/// <summary>
/// Identifies a field type supported by the current durable schema model.
/// </summary>
public enum TypeTag {
    Invalid = 0,
    Boolean = 1,
    Int32 = 2,
    Int64 = 3,
    String = 4,
}
